SHA1, MD5, GUID Generator
=====


Installation
------------

        $ git clone git@github.com:camelloj/encoder.git
        $ cd encoder
        $ bundle install

Start Server
------------

        $ cd encoder
        $ rackup